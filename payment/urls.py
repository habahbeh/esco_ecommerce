from django.urls import path
from . import views

app_name = 'payment'

urlpatterns = [
    # سيتم إضافة المسارات هنا لاحقًا
    # path('process/', views.process_payment, name='process'),
    # path('success/', views.payment_success, name='success'),
    # path('cancel/', views.payment_cancel, name='cancel'),
]